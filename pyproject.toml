[project]
name = "math-wild"
version = "0.3.0"
description = "A Manim-based math visual scene compiler prototype for exploratory math lessons."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "manim>=0.18,<0.21",
    "pillow>=10",
    "numpy>=1.24",
]

[tool.uv]
package = false
